engineering contract: ANY Go/SQL/config change MUST begin by invoking the project-router skill, then LOAD every matched skill and APPLY it while writing (not as an afterthought). Baseline: go-style, errors-logging, code-placement, code-minimalism. Load integration-tests for ANY _test.go (incl. tests alongside a lib); add security/transactions/eventing-outbox/usecase-layout/etc per the dispatch table. Tests: same-package, one _test.go per source file, table-driven slice testCases / row testCase (never tt), cover every failure path, testing/synctest for time (never a now func() clock field, never time.Sleep). No em dash (U+2014) in .go. Before commit run each loaded skill Validation checklist + verify: go build && go vet && go test -race, golangci-lint (curated bar), gofmt -l.
